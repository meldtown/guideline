image: mac2000/fedor

pipelines:
  default:
    - step:
        script:
          - npm run dist -s
  branches:
    master:
      - step:
          script:
            - ls / -la
            - ls /node_modules -la
            - ln -s /node_modules ./node_modules
            - npm run dist -s
            - node deploy.js
