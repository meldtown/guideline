image: mac2000/fedor

pipelines:
  default:
    - step:
        script:
          - npm run dist -s
  branches:
    master:
      - step:
          script:
            - npm run dist -s
            - node deploy.js
